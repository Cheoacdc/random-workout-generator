import React from 'react';

import {
    Box
} from '@mui/material';

export const Logo = () => {
    return (
        <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
            <p>Logo</p>
        </Box>
    );
};